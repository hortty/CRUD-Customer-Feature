# Angular2 + .NET Core 6

Para rodar essa aplicação basta baixar na máquina e rodar o comando "docker-compose up --build", e ela estará pronta para testes.

In order to run this application, download the code and use "docker-compose up --build" and it will be ready to use.
